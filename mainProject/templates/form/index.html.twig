{% extends 'base.html.twig' %}

{% block title %}Hello FormController!{% endblock %}

{% block body %}
    {{ form_start(post_form, {'sttr': {'id': 'custom_post_form'}  }) }}
        {{ form(post_form) }}
    {{ form_end(post_form) }}
{% endblock %}
